/**
 * The blueprint grid background was removed by request. Kept as a no-op so the
 * many call sites don't need touching (and it can be re-enabled here if wanted).
 */
export function BlueprintGrid(_props: { className?: string }) {
  return null;
}
