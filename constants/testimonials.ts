import type { Testimonial } from "@/types";

// Success stories intentionally left blank for now — content is being worked on.
export const testimonials: Testimonial[] = [];
export const parentTestimonials: Testimonial[] = [];
