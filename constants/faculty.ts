import type { FacultyMember } from "@/types";

// Faculty section intentionally left blank for now — to be added after the
// discussion with the panel team.
export const faculty: FacultyMember[] = [];
